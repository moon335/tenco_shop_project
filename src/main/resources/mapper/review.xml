<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tenco.tencoshop.repository.interfaces.ReviewRepository">
	
	<select id="findByType" resultType="com.tenco.tencoshop.dto.ReviewResponseDto">
		<if test="type == 'orderByHeart'">
			select r.*, u.username
			from review_tb as r 
			inner join user_tb as u
			on r.user_id = u.id
			order by heart desc
		</if>
		
		<if test="type == 'orderByRecent'">
			select r.*, u.username
			from review_tb as r 
			inner join user_tb as u
			on r.user_id = u.id
			order by id desc
		</if>
	</select>

	<!-- id를 기반으로 상세페이지 들어가야 됨 -->
	<select id="findDetail" resultType="com.tenco.tencoshop.dto.ReviewResponseDto">
		select r.*, u.username
		from review_tb as r 
		inner join user_tb as u
		on r.user_id = u.id
		where u.id=#{userId}
	</select>
	
	<select id="findByProdCategoryId" resultType="com.tenco.tencoshop.dto.ReviewResponseDto">
		select p.category_id, p.category_id
		from review_tb as r
		inner join product_tb as p
		where r.prod_id=#{prodId};
	</select>
	
</mapper>
